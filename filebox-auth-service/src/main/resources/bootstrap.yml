spring:
  application:
    name: auth-service
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-service
eureka:
  client:
    service-url:
      defaultZone: http://eureka-service8002:8002/eureka/, http://eureka-service8001:8001/eureka/